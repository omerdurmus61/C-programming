#include <stdio.h>
#include <stdlib.h>

int main()
{
    int i=0;
    while (i<=15)
    {
        printf(" %d sayisinin octal= %o decimal = %d hexadecimal = %X\n",i,i,i,i);
        i++;
    }
    return 0;
}
